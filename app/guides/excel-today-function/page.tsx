import type{Metadata}from"next";import{AppLanguage,languageOptions}from"../../i18n";import GuideArticleLayout from"../GuideArticleLayout";import{guideDemos}from"../guideDemos";import{todayContent}from"./content";export const metadata:Metadata={title:"Excel TODAY 函數教學｜今天日期與逾期判斷｜EveryFormula",description:"學習 Excel TODAY，自動日期、剩餘天數與逾期提醒。"};export default function TodayGuidePage({searchParams}:{searchParams?:{lang?:string}}){const r=searchParams?.lang as AppLanguage|undefined;const l=languageOptions.some(o=>o.value===r)?r as AppLanguage:"zh-TW";return <GuideArticleLayout language={l} path="/guides/excel-today-function" content={todayContent[l]} demo={guideDemos.today[l]}/>;}
