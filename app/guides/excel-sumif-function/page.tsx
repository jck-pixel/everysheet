import type{Metadata}from"next";import{AppLanguage,languageOptions}from"../../i18n";import GuideArticleLayout from"../GuideArticleLayout";import{sumifContent}from"./content";import{sumifExamples}from"./examples";export const metadata:Metadata={title:"Excel SUMIF 函數教學｜單一條件加總｜EveryFormula",description:"學習 Excel SUMIF，依部門、商品、門檻與文字條件加總。"};export default function SumifGuidePage({searchParams}:{searchParams?:{lang?:string}}){const r=searchParams?.lang as AppLanguage|undefined;const l=languageOptions.some(o=>o.value===r)?r as AppLanguage:"zh-TW";return <GuideArticleLayout language={l} path="/guides/excel-sumif-function" content={sumifContent[l]} exampleDemos={sumifExamples[l]}/>;}
