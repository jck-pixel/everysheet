import type{Metadata}from"next";import{AppLanguage,languageOptions}from"../../i18n";import GuideArticleLayout from"../GuideArticleLayout";import{matchContent}from"./content";import{matchExamples}from"./examples";
export const metadata:Metadata={title:"Excel MATCH 函數教學｜尋找資料位置｜EverySheet",description:"學習 Excel MATCH 精確比對、相對位置及搭配 INDEX 動態查找。"};export default function MatchGuidePage({searchParams}:{searchParams?:{lang?:string}}){const r=searchParams?.lang as AppLanguage|undefined;const l=languageOptions.some(o=>o.value===r)?r as AppLanguage:"zh-TW";return <GuideArticleLayout language={l} path="/guides/excel-match-function" content={matchContent[l]} exampleDemos={matchExamples[l]}/>;}
