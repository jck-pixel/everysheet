import type{Metadata}from"next";import{AppLanguage,languageOptions}from"../../i18n";import GuideArticleLayout from"../GuideArticleLayout";import{guideDemos}from"../guideDemos";import{countifContent}from"./content";export const metadata:Metadata={title:"Excel COUNTIF 函數教學｜單一條件計數｜EveryFormula",description:"學習 Excel COUNTIF，統計及格、完成與文字條件筆數。"};export default function CountifGuidePage({searchParams}:{searchParams?:{lang?:string}}){const r=searchParams?.lang as AppLanguage|undefined;const l=languageOptions.some(o=>o.value===r)?r as AppLanguage:"zh-TW";return <GuideArticleLayout language={l} path="/guides/excel-countif-function" content={countifContent[l]} demo={guideDemos.countif[l]}/>;}
